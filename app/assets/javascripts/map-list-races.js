// listen for scroll
var positionElementInPage = $('#map').offset().top;
$(window).scroll(
  function() {
    // if ($(window).scrolldown(){
    //   // fixed
    //   $('#map').addClass("floatable");
    // } else {
      // relative
      // $('#map').removeClass("floatable");
    // }
  s}
);
