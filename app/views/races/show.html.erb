<% content_for :meta_title, "#{@race.name} - Friends Challenge" %>
<% content_for :meta_description, truncate(@race.description, length: 200) %>
<%= render 'shared/race_show_base', race: @race, race_group: @race_group, group_membership: @group_membership, view_mode: "race" %>
